# Руководство по публикации ChatList

Пошаговая инструкция для публикации приложения на GitHub Release и GitHub Pages.

---

## 📋 Содержание

1. [Подготовка репозитория](#1-подготовка-репозитория)
2. [Настройка GitHub Pages](#2-настройка-github-pages)
3. [Создание нового релиза](#3-создание-нового-релиза)
4. [Автоматическая сборка (CI/CD)](#4-автоматическая-сборка-cicd)
5. [Ручная публикация релиза](#5-ручная-публикация-релиза)

---

## 1. Подготовка репозитория

### Шаг 1.1: Убедитесь, что все изменения закоммичены

```powershell
# Проверка статуса
git status

# Добавление всех файлов
git add .

# Коммит изменений
git commit -m "Подготовка к релизу v1.0.0"
```

### Шаг 1.2: Отправьте изменения на GitHub

```powershell
# Пуш в основную ветку
git push origin main
```

### Шаг 1.3: Проверьте структуру файлов

Убедитесь, что в репозитории есть:
- ✅ `.github/workflows/release.yml` — для автоматических релизов
- ✅ `.github/workflows/pages.yml` — для GitHub Pages
- ✅ `docs/index.html` — лендинг страница
- ✅ `CHANGELOG.md` — история изменений
- ✅ `LICENSE` — лицензия

---

## 2. Настройка GitHub Pages

### Шаг 2.1: Откройте настройки репозитория

1. Перейдите в репозиторий на GitHub: `https://github.com/KHaberland/ChatList`
2. Нажмите **Settings** (⚙️ Настройки)
3. В левом меню выберите **Pages**

### Шаг 2.2: Настройте источник публикации

**Вариант A: Через GitHub Actions (рекомендуется)**

1. В разделе **Build and deployment** → **Source** выберите **GitHub Actions**
2. Workflow `.github/workflows/pages.yml` автоматически задеплоит папку `docs/`

**Вариант B: Напрямую из ветки**

1. **Source** → выберите **Deploy from a branch**
2. **Branch** → выберите `main` и папку `/docs`
3. Нажмите **Save**

### Шаг 2.3: Запустите первый деплой

```powershell
# Внесите любое изменение в docs/ и запушьте
git add docs/
git commit -m "Обновление лендинга"
git push origin main
```

### Шаг 2.4: Проверьте результат

Через 1-2 минуты ваш сайт будет доступен по адресу:
```
https://khaberland.github.io/ChatList/
```

---

## 3. Создание нового релиза

### Способ 1: Через теги (автоматический релиз)

GitHub Actions автоматически создаст релиз при пуше тега.

```powershell
# 1. Обновите версию в файлах
# - version.py: __version__ = "1.0.1"
# - ChatList.iss: #define MyAppVersion "1.0.1"
# - docs/index.html: обновите версии в бейджах

# 2. Закоммитьте изменения
git add .
git commit -m "Обновление версии до 1.0.1"

# 3. Создайте тег
git tag -a v1.0.1 -m "Релиз версии 1.0.1"

# 4. Отправьте тег на GitHub
git push origin v1.0.1
```

После пуша тега:
1. GitHub Actions запустит сборку
2. Создастся EXE файл через PyInstaller
3. Создастся инсталлятор через Inno Setup
4. Автоматически опубликуется релиз с файлами

### Способ 2: Ручное создание релиза

См. раздел [5. Ручная публикация релиза](#5-ручная-публикация-релиза)

---

## 4. Автоматическая сборка (CI/CD)

### Как работает автоматизация

Файл `.github/workflows/release.yml` выполняет:

1. **Триггер**: Запуск при пуше тега `v*` (например, `v1.0.0`)
2. **Сборка**: 
   - Установка Python 3.12
   - Установка зависимостей из `requirements.txt`
   - Сборка EXE через PyInstaller
   - Сборка инсталлятора через Inno Setup
3. **Публикация**: Создание GitHub Release с файлами

### Проверка статуса сборки

1. Перейдите в **Actions** в репозитории
2. Найдите запуск "Build and Release"
3. Проверьте логи на наличие ошибок

### Устранение проблем

**Ошибка: PyInstaller не найден**
```yaml
# В release.yml добавьте:
pip install pyinstaller
```

**Ошибка: Inno Setup не найден**
```yaml
# Workflow скачивает Inno Setup автоматически
# Проверьте путь: "C:\Program Files (x86)\Inno Setup 6\ISCC.exe"
```

---

## 5. Ручная публикация релиза

Если вы предпочитаете публиковать вручную:

### Шаг 5.1: Соберите приложение локально

```powershell
# Активируйте виртуальное окружение
.\venv\Scripts\Activate.ps1

# Соберите EXE
pyinstaller --onefile --windowed --icon=app.ico --name=ChatList main.py

# Соберите инсталлятор (нужен Inno Setup)
& "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" ChatList.iss
```

### Шаг 5.2: Создайте релиз на GitHub

1. Перейдите в **Releases** → **Draft a new release**
2. Нажмите **Choose a tag** → введите `v1.0.0` → **Create new tag**
3. **Release title**: `ChatList v1.0.0`
4. **Description**: Скопируйте из CHANGELOG.md
5. Прикрепите файлы:
   - `dist/ChatList.exe`
   - `installer/ChatList-1.0.0-Setup.exe`
6. Нажмите **Publish release**

---

## 📝 Чеклист перед релизом

- [ ] Версия обновлена в `version.py`
- [ ] Версия обновлена в `ChatList.iss`
- [ ] Версия обновлена в `docs/index.html`
- [ ] CHANGELOG.md содержит описание изменений
- [ ] Все тесты проходят
- [ ] Приложение протестировано локально
- [ ] Изменения закоммичены и запушены

---

## 🔗 Полезные ссылки

- [GitHub Actions документация](https://docs.github.com/en/actions)
- [GitHub Pages документация](https://docs.github.com/en/pages)
- [GitHub Releases документация](https://docs.github.com/en/repositories/releasing-projects-on-github)
- [Inno Setup документация](https://jrsoftware.org/ishelp/)

---

## ❓ FAQ

**Q: Как откатить релиз?**
```powershell
# Удалите тег локально
git tag -d v1.0.0

# Удалите тег на GitHub
git push origin --delete v1.0.0
```
Затем удалите релиз вручную в интерфейсе GitHub.

**Q: Как сделать предварительный релиз (pre-release)?**

В workflow измените:
```yaml
prerelease: true
```
Или в интерфейсе GitHub отметьте "Set as a pre-release".

**Q: Как добавить автоматическое обновление версии в лендинге?**

Добавьте в workflow шаг, который заменяет версию в `docs/index.html` перед деплоем.

